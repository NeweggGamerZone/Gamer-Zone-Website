.DS_Store
Thumbs.db
*.log
GithubToken.txt
githubToken.txt
token.txt
assets/ImageAssets/

# raw SENET exports (visitor data) — keep private
assets/Leaderboard/
*.xlsx

# keep admin + raw referral data out of the public repo
gz-referrals.html
data/referrals.json
archive/

# local dev/build artifacts
node_modules/
out/
.worktrees/

# tools/audit/: fetched headless-Chrome shared libs (see tools/audit/setup.sh) —
# re-fetched per environment, not portable/versioned
tools/audit/.deps/
